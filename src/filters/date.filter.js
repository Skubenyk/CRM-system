export default function dateFilter(value, format = "date") {
  if (format === "date") {
    // Форматування дати в потрібний вам спосіб
  } else if (format === "time") {
    // Форматування часу в потрібний вам спосіб
  } else {
    // Інше форматування за замовчуванням
  }
  return value;
}
